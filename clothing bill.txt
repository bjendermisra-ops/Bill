<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes">
  <title>Clothing & Fabric Store Bill Generator</title>
  <!-- Google Fonts & Font Awesome -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,300;14..32,400;14..32,600;14..32,700;14..32,800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <!-- html2canvas for PNG/JPG and jspdf for PDF -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #fef8f0 0%, #fdf3e8 100%);
      color: #2d2a24;
      scroll-behavior: smooth;
    }

    .container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 24px;
    }

    /* navbar */
    .navbar {
      background: rgba(255, 255, 255, 0.98);
      backdrop-filter: blur(8px);
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
      position: sticky;
      top: 0;
      z-index: 100;
      border-bottom: 1px solid rgba(219, 112, 147, 0.2);
    }

    .nav-container {
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      padding: 14px 24px;
      max-width: 1400px;
      margin: 0 auto;
    }

    .logo {
      font-size: 1.8rem;
      font-weight: 800;
      background: linear-gradient(135deg, #db7093, #e6a0b5);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }

    .logo i {
      background: none;
      color: #db7093;
    }

    .nav-links {
      display: flex;
      gap: 32px;
      list-style: none;
      flex-wrap: wrap;
    }

    .nav-links a {
      text-decoration: none;
      font-weight: 600;
      color: #4a3b2c;
      transition: 0.3s;
      cursor: pointer;
    }

    .nav-links a:hover, .nav-links a.active {
      color: #db7093;
    }

    .btn-primary {
      background: linear-gradient(135deg, #db7093, #e6a0b5);
      border: none;
      padding: 10px 28px;
      border-radius: 40px;
      font-weight: 700;
      color: white;
      cursor: pointer;
      transition: 0.3s;
      box-shadow: 0 4px 12px rgba(219, 112, 147, 0.3);
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(219, 112, 147, 0.4);
    }

    /* Hero Section */
    .hero {
      padding: 50px 0 40px;
      background: linear-gradient(145deg, #fff5f0 0%, #ffffff 100%);
    }

    .hero-badge {
      background: linear-gradient(135deg, #fee8f0, #ffe0ea);
      display: inline-block;
      padding: 8px 20px;
      border-radius: 40px;
      font-weight: 600;
      color: #db7093;
      margin-bottom: 20px;
    }

    .hero h1 {
      font-size: 2.8rem;
      font-weight: 800;
      background: linear-gradient(120deg, #c95a7a, #db7093);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      margin-bottom: 20px;
    }

    /* Bill Panel */
    .bill-panel {
      background: white;
      border-radius: 40px;
      padding: 35px;
      margin: 40px 0;
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.08);
    }

    .fashion-header {
      background: linear-gradient(135deg, #fff5f0, #ffeef4);
      border-radius: 28px;
      padding: 20px;
      margin-bottom: 30px;
      border-left: 5px solid #db7093;
    }

    .form-row {
      display: flex;
      flex-wrap: wrap;
      gap: 24px;
      margin-bottom: 28px;
    }

    .input-group {
      flex: 1;
      min-width: 200px;
    }

    .input-group label {
      font-weight: 700;
      display: block;
      margin-bottom: 8px;
      color: #c95a7a;
      font-size: 0.9rem;
    }

    input, select {
      width: 100%;
      padding: 12px 18px;
      border-radius: 28px;
      border: 1.5px solid #ffe0ea;
      font-family: inherit;
      transition: 0.2s;
      font-size: 0.95rem;
      background: #ffffff;
    }

    input:focus, select:focus {
      border-color: #db7093;
      outline: none;
      box-shadow: 0 0 0 3px rgba(219, 112, 147, 0.1);
    }

    /* Table Styles - Professional Bill Format */
    .table-wrapper {
      overflow-x: auto;
      margin: 25px 0;
      border-radius: 24px;
      border: 1px solid #ffe0ea;
      background: white;
      box-shadow: 0 2px 8px rgba(0,0,0,0.02);
    }

    .fashion-table {
      width: 100%;
      border-collapse: collapse;
      font-size: 0.9rem;
    }

    .fashion-table th {
      background: #db7093;
      color: white;
      padding: 15px 12px;
      font-weight: 700;
      text-align: center;
      font-size: 0.95rem;
    }

    .fashion-table td {
      padding: 12px 10px;
      text-align: center;
      border-bottom: 1px solid #ffe0ea;
      vertical-align: middle;
    }

    .fashion-table input {
      width: 100%;
      min-width: 90px;
      padding: 8px 10px;
      border-radius: 20px;
      border: 1px solid #ffe0ea;
      text-align: center;
      font-size: 0.9rem;
    }

    .fashion-table input:focus {
      border-color: #db7093;
      box-shadow: 0 0 0 2px rgba(219,112,147,0.2);
    }

    .remove-row {
      background: #ff8c5a;
      border: none;
      color: white;
      padding: 6px 14px;
      border-radius: 30px;
      cursor: pointer;
      font-size: 0.75rem;
      transition: 0.2s;
    }

    .remove-row:hover {
      background: #e67e22;
      transform: scale(0.95);
    }

    .add-item-btn {
      background: #fff5f0;
      border: 2px dashed #db7093;
      padding: 12px 24px;
      border-radius: 40px;
      cursor: pointer;
      font-weight: 600;
      margin-top: 15px;
      width: 100%;
      transition: 0.2s;
      color: #db7093;
      font-size: 1rem;
    }

    .add-item-btn:hover {
      background: #ffeef4;
      border-color: #e6a0b5;
    }

    /* Live Bill Preview */
    .live-bill-preview {
      background: #fffef7;
      border-radius: 28px;
      padding: 30px;
      margin-top: 35px;
      border: 2px solid #ffe0ea;
      box-shadow: 0 8px 20px rgba(0,0,0,0.05);
    }

    .preview-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      border-bottom: 2px dashed #db7093;
      padding-bottom: 15px;
      margin-bottom: 25px;
    }

    .download-buttons {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
    }

    .download-btn {
      background: #2c3e50;
      color: white;
      padding: 8px 20px;
      border-radius: 32px;
      border: none;
      font-weight: 600;
      cursor: pointer;
      transition: 0.2s;
      font-size: 0.9rem;
    }

    .download-btn.pdf { background: #e74c3c; }
    .download-btn.png { background: #27ae60; }
    .download-btn.jpg { background: #f39c12; }

    .download-btn:hover {
      transform: translateY(-2px);
      filter: brightness(1.05);
    }

    .bill-content {
      font-family: 'Inter', monospace;
      background: white;
      padding: 25px;
      border-radius: 20px;
    }

    /* Popular Items Section */
    .popular-items {
      margin: 30px 0;
    }

    .items-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
      gap: 16px;
      margin-top: 20px;
    }

    .item-card {
      background: white;
      border-radius: 20px;
      padding: 18px 12px;
      text-align: center;
      cursor: pointer;
      transition: 0.2s;
      border: 1px solid #ffe0ea;
    }

    .item-card:hover {
      transform: translateY(-3px);
      border-color: #db7093;
      box-shadow: 0 8px 20px rgba(219, 112, 147, 0.1);
      background: #fffaf5;
    }

    .item-card i {
      font-size: 2rem;
      color: #db7093;
      margin-bottom: 10px;
    }

    .item-card .item-price {
      font-weight: 700;
      color: #c95a7a;
      margin-top: 5px;
    }

    /* Footer */
    .footer-policies {
      background: #2d2418;
      color: #f5e6d3;
      padding: 40px 0 20px;
      margin-top: 60px;
    }

    .policy-links {
      display: flex;
      gap: 30px;
      flex-wrap: wrap;
      justify-content: center;
      margin-bottom: 20px;
    }

    .policy-links a {
      color: #e6c9a8;
      text-decoration: none;
      transition: 0.2s;
    }

    .policy-links a:hover {
      color: #db7093;
    }

    .copyright {
      text-align: center;
      font-size: 0.8rem;
      opacity: 0.8;
    }

    @media (max-width: 768px) {
      .hero h1 { font-size: 1.8rem; }
      .bill-panel { padding: 24px; }
      .fashion-table th, .fashion-table td { font-size: 0.75rem; padding: 8px; }
      .fashion-table input { min-width: 70px; }
    }

    .store-badge {
      background: #ffeef4;
      display: inline-block;
      padding: 8px 20px;
      border-radius: 40px;
      font-weight: 600;
      font-size: 0.9rem;
      color: #db7093;
    }
  </style>
</head>
<body>

<nav class="navbar">
  <div class="nav-container">
    <div class="logo"><i class="fas fa-tshirt"></i> SmartBills<span style="color:#db7093;">.Pro</span></div>
    <ul class="nav-links">
     <li><a href="/">Home</a></li>
      <li><a href="onlineservicebill.html">Service Bill</a></li>
       <li><a href="kiranastorebill.html">Kirana Bill</a></li>
       <li><a href="Classesbill.html">Classes Bill</a></li>
       
      <li><a href="Electronicbill.html">Electronic Bill</a></li>
      <li><a href="Vegetablebill.html">Vegetable Bill</a></li>
    </ul>
    <button class="btn-primary" id="scrollToBillBtn"><i class="fas fa-receipt"></i> बिल बनाएं</button>
  </div>
</nav>

<main>
  <section id="home" class="hero">
    <div class="container">
      <div class="hero-badge">
        <i class="fas fa-tshirt"></i> कपड़े एवं फैब्रिक स्टोर बिल जनरेटर
      </div>
      <h1>Clothing & Fabric Store Bill Generator <br><span style="font-size: 1.8rem;">कुर्ता, जींस, शर्ट, साड़ी, फैब्रिक - सभी का बिल टेबल फॉर्मेट में</span></h1>
      <p style="font-size: 1.1rem; max-width: 800px;">प्रोफेशनल कपड़े की दुकान का बिल सेकंडों में। Fabric/Products, Size, Quantity, Rate, Total के साथ टेबल फॉर्मेट। PDF/PNG/JPG डाउनलोड। 100% फ्री!</p>
    </div>
  </section>

  <div class="container">
    <!-- Popular Items Section -->
    <div class="popular-items">
      <h3 style="margin-bottom: 15px; color: #c95a7a;"><i class="fas fa-tag"></i> लोकप्रिय कपड़े एवं फैब्रिक (एक क्लिक में जोड़ें)</h3>
      <div class="items-grid" id="popularItemsGrid"></div>
    </div>

    <!-- Main Bill Generator Panel -->
    <div class="bill-panel" id="generatorSection">
      <div class="fashion-header">
        <span class="store-badge"><i class="fas fa-store"></i> कपड़े की दुकान बिल - टेबल फॉर्मेट</span>
        <p style="margin-top: 12px; color: #c95a7a;">नीचे दी गई टेबल में Fabric/Products, Size, Quantity, Rate भरें और लाइव बिल देखें</p>
      </div>

      <!-- Shopkeeper & Customer Details -->
      <div class="form-row">
        <div class="input-group">
          <label><i class="fas fa-store"></i> दुकानदार का नाम / दुकान का नाम</label>
          <input type="text" id="shopName" value="फैशन हब कपड़े भंडार" placeholder="दुकान का नाम">
        </div>
        <div class="input-group">
          <label><i class="fas fa-user"></i> ग्राहक का नाम</label>
          <input type="text" id="customerName" value="प्रियंका सिंह" placeholder="ग्राहक नाम">
        </div>
        <div class="input-group">
          <label><i class="fas fa-phone"></i> मोबाइल नंबर</label>
          <input type="text" id="customerPhone" value="9876543210" placeholder="मोबाइल नंबर">
        </div>
        <div class="input-group">
          <label><i class="fas fa-calendar"></i> बिल तारीख</label>
          <input type="date" id="billDate" value="2025-03-26">
        </div>
      </div>

      <div class="form-row">
        <div class="input-group">
          <label><i class="fas fa-hashtag"></i> बिल नंबर</label>
          <input type="text" id="billNumber" value="CLOTH-2025-001">
        </div>
        <div class="input-group">
          <label><i class="fas fa-tag"></i> सीजन / कलेक्शन</label>
          <input type="text" id="season" value="समर कलेक्शन 2025">
        </div>
        <div class="input-group">
          <label><i class="fas fa-percent"></i> GST %</label>
          <input type="number" id="gstPercent" value="5" step="0.5">
        </div>
      </div>

      <!-- Items Table with Fabric/Products, Size, Quantity, Rate, Total -->
      <div class="table-wrapper">
        <table class="fashion-table" id="itemsTable">
          <thead>
            <tr>
              <th>Fabric / Products</th>
              <th>Size</th>
              <th>Quantity</th>
              <th>Rate (₹)</th>
              <th>Total (₹)</th>
              <th style="width:70px"></th>
            </tr>
          </thead>
          <tbody id="tableBody"></tbody>
        </table>
      </div>
      <button class="add-item-btn" id="addItemBtn"><i class="fas fa-plus-circle"></i> + नया उत्पाद जोड़ें</button>

      <!-- Extra Fields -->
      <div class="form-row" style="margin-top: 25px;">
        <div class="input-group">
          <label><i class="fas fa-tag"></i> डिस्काउंट (₹)</label>
          <input type="number" id="discountAmount" value="0" placeholder="डिस्काउंट ₹">
        </div>
        <div class="input-group">
          <label><i class="fas fa-percent"></i> डिस्काउंट %</label>
          <input type="number" id="discountPercent" value="0" step="1" placeholder="डिस्काउंट %">
        </div>
        <div class="input-group">
          <label><i class="fas fa-sticky-note"></i> अतिरिक्त नोट</label>
          <input type="text" id="extraNote" value="धन्यवाद! एक्सचेंज 7 दिन में | GST शामिल">
        </div>
      </div>

      <!-- LIVE BILL PREVIEW with Download -->
      <div class="live-bill-preview">
        <div class="preview-header">
          <h3><i class="fas fa-eye"></i> लाइव बिल प्रीव्यू (टेबल फॉर्मेट)</h3>
          <div class="download-buttons">
            <button class="download-btn pdf" id="downloadPDFBtn"><i class="fas fa-file-pdf"></i> PDF</button>
            <button class="download-btn png" id="downloadPNGBtn"><i class="fas fa-image"></i> PNG</button>
            <button class="download-btn jpg" id="downloadJPGBtn"><i class="fas fa-camera"></i> JPG</button>
          </div>
        </div>
        <div id="billPreviewContent" class="bill-content">
          <div style="text-align:center; padding:30px;">बिल लोड हो रहा है...</div>
        </div>
      </div>
    </div>
  </div>

  <!-- About & Contact -->
  <div class="container" id="about" style="margin:30px auto;">
    <div style="background:#fff; border-radius:32px; padding:32px; border:1px solid #ffe0ea;">
      <h2><i class="fas fa-address-card"></i> About Clothing Bill Tool</h2>
      <p style="margin-top: 16px;">कपड़े की दुकान बिल जनरेटर - फैशन स्टोर, बुटीक, कपड़े की दुकानों के लिए मुफ्त बिलिंग टूल। Fabric/Products, Size, Quantity, Rate, Total के साथ प्रोफेशनल टेबल फॉर्मेट। PDF, PNG, JPG में डाउनलोड करें। 100% फ्री!</p>
    </div>
  </div>

  <div class="container" id="contact" style="margin-bottom:40px;">
    <div style="background:#fff; border-radius:32px; padding:32px; border:1px solid #ffe0ea;">
      <h2><i class="fas fa-envelope"></i> Contact Us</h2>
      <p style="margin: 16px 0;">Email: clothing@smartbills.pro | फोन: +91-98765-43210 | WhatsApp सपोर्ट 24x7</p>
    </div>
  </div>
</main>

<footer class="footer-policies">
  <div class="container policy-links">
    <a href="#">Privacy Policy</a>
    <a href="#">Terms of Service</a>
    <a href="#">Refund Policy</a>
    <a href="#">Exchange Policy</a>
    <a href="#">Cancellation Policy</a>
  </div>
  <div class="container copyright">© 2025 StyleBills.Pro | कपड़े एवं फैब्रिक स्टोर बिल जनरेटर | 100% Free | PDF/PNG/JPG Download</div>
</footer>

<script>
  // Clothing Items Data with Fabric/Products, Size, Quantity, Rate
  let clothingItems = [
    { name: "कुर्ता (सिल्क)", size: "XL", qty: 2, price: 850 },
    { name: "जींस (ब्लू)", size: "32", qty: 1, price: 1200 },
    { name: "शर्ट (कॉटन)", size: "L", qty: 1, price: 899 },
    { name: "साड़ी (सिल्क)", size: "FREE", qty: 1, price: 1500 }
  ];

  const tableBody = document.getElementById("tableBody");
  const shopInput = document.getElementById("shopName");
  const customerInput = document.getElementById("customerName");
  const customerPhoneInput = document.getElementById("customerPhone");
  const billDateInput = document.getElementById("billDate");
  const billNumberInput = document.getElementById("billNumber");
  const seasonInput = document.getElementById("season");
  const gstPercentInput = document.getElementById("gstPercent");
  const extraNoteInput = document.getElementById("extraNote");
  const discountAmountInput = document.getElementById("discountAmount");
  const discountPercentInput = document.getElementById("discountPercent");
  const previewDiv = document.getElementById("billPreviewContent");

  function renderTable() {
    tableBody.innerHTML = "";
    clothingItems.forEach((item, idx) => {
      const row = document.createElement("tr");
      row.innerHTML = `
        <td><input type="text" class="item-name" value="${escapeHtml(item.name)}" data-idx="${idx}" style="min-width:150px;"></td>
        <td><input type="text" class="item-size" value="${item.size}" data-idx="${idx}" style="width:80px;" placeholder="साइज"></td>
        <td><input type="number" class="item-qty" value="${item.qty}" step="1" data-idx="${idx}" style="width:80px;"></td>
        <td><input type="number" class="item-price" value="${item.price}" step="50" data-idx="${idx}" style="width:110px;"></td>
        <td class="item-total">₹${(item.qty * item.price).toFixed(2)}</td>
        <td><button class="remove-row" data-idx="${idx}"><i class="fas fa-trash-alt"></i></button></td>
      `;
      tableBody.appendChild(row);
    });
    attachTableEvents();
    updateLiveBill();
  }

  function escapeHtml(str) { return str; }

  function attachTableEvents() {
    document.querySelectorAll(".item-name, .item-size, .item-qty, .item-price").forEach(inp => {
      inp.removeEventListener("input", handleItemChange);
      inp.addEventListener("input", handleItemChange);
    });
    document.querySelectorAll(".remove-row").forEach(btn => {
      btn.removeEventListener("click", handleRemoveItem);
      btn.addEventListener("click", handleRemoveItem);
    });
  }

  function handleItemChange(e) {
    const idx = e.target.getAttribute("data-idx");
    if (idx !== null) {
      const nameInput = document.querySelector(`.item-name[data-idx='${idx}']`);
      const sizeInput = document.querySelector(`.item-size[data-idx='${idx}']`);
      const qtyInput = document.querySelector(`.item-qty[data-idx='${idx}']`);
      const priceInput = document.querySelector(`.item-price[data-idx='${idx}']`);
      if (nameInput && qtyInput && priceInput) {
        clothingItems[idx].name = nameInput.value;
        clothingItems[idx].size = sizeInput ? sizeInput.value : "FREE";
        clothingItems[idx].qty = parseFloat(qtyInput.value) || 0;
        clothingItems[idx].price = parseFloat(priceInput.value) || 0;
      }
    }
    updateTotalsInTable();
    updateLiveBill();
  }

  function handleRemoveItem(e) {
    const idx = e.currentTarget.getAttribute("data-idx");
    if (idx !== null) {
      clothingItems.splice(parseInt(idx), 1);
      renderTable();
    }
  }

  function updateTotalsInTable() {
    const rows = tableBody.querySelectorAll("tr");
    rows.forEach((row, idx) => {
      if (clothingItems[idx]) {
        const totalCell = row.querySelector(".item-total");
        const totalAmt = clothingItems[idx].qty * clothingItems[idx].price;
        if (totalCell) totalCell.innerText = `₹${totalAmt.toFixed(2)}`;
      }
    });
  }

  function getBillData() {
    let items = clothingItems.map(i => ({
      name: i.name,
      size: i.size || "FREE",
      quantity: i.qty,
      price: i.price,
      total: i.qty * i.price
    }));
    let subtotal = items.reduce((sum, i) => sum + i.total, 0);
    let discountAmt = parseFloat(discountAmountInput.value) || 0;
    let discountPercent = parseFloat(discountPercentInput.value) || 0;
    let discountFromPercent = subtotal * (discountPercent / 100);
    let totalDiscount = discountAmt + discountFromPercent;
    let afterDiscount = subtotal - totalDiscount;
    let gstPercent = parseFloat(gstPercentInput.value) || 0;
    let gstAmount = afterDiscount * (gstPercent / 100);
    let grandTotal = afterDiscount + gstAmount;
    return { items, subtotal, totalDiscount, afterDiscount, gstPercent, gstAmount, grandTotal, discountAmt, discountPercent };
  }

  function updateLiveBill() {
    const shop = shopInput.value || "कपड़े की दुकान";
    const customer = customerInput.value || "ग्राहक";
    const phone = customerPhoneInput.value || "";
    const date = billDateInput.value || new Date().toISOString().slice(0,10);
    const billNo = billNumberInput.value || "CLOTH-INV";
    const season = seasonInput.value || "";
    const extraNote = extraNoteInput.value || "";
    const { items, subtotal, totalDiscount, afterDiscount, gstPercent, gstAmount, grandTotal } = getBillData();

    let itemsHtml = `<table style="width:100%; border-collapse: collapse; margin:20px 0; font-size:0.9rem;">
      <thead>
        <tr style="background:#db7093; color:white;">
          <th style="padding:12px;">Fabric / Products</th>
          <th>Size</th>
          <th>Qty</th>
          <th>Rate (₹)</th>
          <th>Total (₹)</th>
        </tr>
      </thead>
      <tbody>`;
    items.forEach(it => {
      itemsHtml += `<tr>
        <td style="padding:10px; border-bottom:1px solid #ffe0ea; text-align:left;">${it.name}</td>
        <td style="text-align:center;">${it.size}</td>
        <td style="text-align:center;">${it.quantity}</td>
        <td style="text-align:center;">₹${it.price.toFixed(2)}</td>
        <td style="text-align:center;">₹${it.total.toFixed(2)}</td>
      </tr>`;
    });
    itemsHtml += `</tbody></table>`;

    const discountHtml = totalDiscount > 0 ? `<div>डिस्काउंट: - ₹${totalDiscount.toFixed(2)}</div>` : '';

    const billHtml = `
      <div style="max-width:100%;">
        <div style="text-align:center; border-bottom:2px dashed #db7093; margin-bottom:20px; padding-bottom:15px;">
          <h2 style="color:#db7093; margin-bottom:8px;"><i class="fas fa-tshirt"></i> ${shop}</h2>
          <p><strong>🧾 बिल नं:</strong> ${billNo} &nbsp;|&nbsp; <strong>📅 तारीख:</strong> ${date}</p>
          <p><strong>👤 ग्राहक:</strong> ${customer} ${phone ? `&nbsp;|&nbsp; 📞 ${phone}` : ''}</p>
          ${season ? `<p><strong>🌸 ${season}</strong></p>` : ''}
        </div>
        ${itemsHtml}
        <div style="text-align:right; margin-top:20px;">
          <div>कुल मूल्य: ₹${subtotal.toFixed(2)}</div>
          ${totalDiscount > 0 ? discountHtml : ''}
          ${totalDiscount > 0 ? `<div>डिस्काउंट के बाद: ₹${afterDiscount.toFixed(2)}</div>` : ''}
          ${gstPercent > 0 ? `<div>GST (${gstPercent}%): ₹${gstAmount.toFixed(2)}</div>` : ''}
          <div style="font-size:1.4rem; font-weight:800; border-top:2px solid #db7093; margin-top:10px; padding-top:10px; color:#db7093;">कुल भुगतान: ₹${grandTotal.toFixed(2)}</div>
          <div style="margin-top:15px; font-size:0.85rem; color:#c95a7a;"><i class="fas fa-info-circle"></i> ${extraNote}</div>
          <div style="margin-top:12px; font-size:0.7rem; color:#b87a6a;">✨ StyleBills.Pro - कपड़े की दुकान बिल जनरेटर | मुफ्त & प्रोफेशनल</div>
        </div>
      </div>
    `;
    previewDiv.innerHTML = billHtml;
  }

  // Add new item button
  document.getElementById("addItemBtn").addEventListener("click", () => {
    clothingItems.push({ name: "नया कपड़ा / फैब्रिक", size: "M", qty: 1, price: 499 });
    renderTable();
  });

  // Live update on all input fields
  const liveFields = [shopInput, customerInput, customerPhoneInput, billDateInput, billNumberInput, seasonInput, gstPercentInput, extraNoteInput, discountAmountInput, discountPercentInput];
  liveFields.forEach(field => {
    field.addEventListener("input", () => updateLiveBill());
  });

  // Popular Clothing & Fabric Items
  const popularItems = [
    { name: "कुर्ता (सिल्क)", size: "XL", qty: 1, price: 850, icon: "fas fa-tshirt" },
    { name: "जींस (ब्लू)", size: "32", qty: 1, price: 1200, icon: "fas fa-jeans" },
    { name: "शर्ट (कॉटन)", size: "L", qty: 1, price: 899, icon: "fas fa-shirt" },
    { name: "साड़ी (सिल्क)", size: "FREE", qty: 1, price: 1500, icon: "fas fa-female" },
    { name: "टी-शर्ट (कॉटन)", size: "M", qty: 2, price: 399, icon: "fas fa-tshirt" },
    { name: "सूट (पंजाबी)", size: "L", qty: 1, price: 2200, icon: "fas fa-user-tie" },
    { name: "फैब्रिक (सिल्क)", size: "मीटर", qty: 3, price: 250, icon: "fas fa-cut" },
    
  ];

  const popularGrid = document.getElementById("popularItemsGrid");
  popularItems.forEach(item => {
    const card = document.createElement("div");
    card.className = "item-card";
    card.innerHTML = `<i class="${item.icon}"></i><div style="font-weight:600;">${item.name}</div><div class="item-price">₹${item.price}</div><small>साइज: ${item.size}</small>`;
    card.addEventListener("click", () => {
      clothingItems.push({ name: item.name, size: item.size, qty: item.qty, price: item.price });
      renderTable();
      document.getElementById("generatorSection").scrollIntoView({ behavior: "smooth" });
    });
    popularGrid.appendChild(card);
  });

  // Download Functions
  async function captureBill() {
    const element = document.getElementById("billPreviewContent");
    if (!element) return null;
    const originalBg = element.style.backgroundColor;
    element.style.backgroundColor = "white";
    const canvas = await html2canvas(element, { scale: 2.5, backgroundColor: "#ffffff", logging: false });
    element.style.backgroundColor = originalBg;
    return canvas;
  }

  document.getElementById("downloadPNGBtn").addEventListener("click", async () => {
    const canvas = await captureBill();
    if(canvas) { const link = document.createElement("a"); link.download = "Clothing_Bill.png"; link.href = canvas.toDataURL("image/png"); link.click(); }
  });
  
  document.getElementById("downloadJPGBtn").addEventListener("click", async () => {
    const canvas = await captureBill();
    if(canvas) { const link = document.createElement("a"); link.download = "Clothing_Bill.jpg"; link.href = canvas.toDataURL("image/jpeg", 0.95); link.click(); }
  });
  
  document.getElementById("downloadPDFBtn").addEventListener("click", async () => {
    const canvas = await captureBill();
    if(canvas) {
      const { jsPDF } = window.jspdf;
      const imgData = canvas.toDataURL("image/png");
      const pdf = new jsPDF("p", "mm", "a4");
      const imgWidth = 190;
      const imgHeight = (canvas.height * imgWidth) / canvas.width;
      pdf.addImage(imgData, "PNG", 10, 10, imgWidth, imgHeight);
      pdf.save("Clothing_Store_Bill.pdf");
    }
  });

  document.getElementById("scrollToBillBtn").addEventListener("click", () => document.getElementById("generatorSection").scrollIntoView({ behavior: "smooth" }));

  // Initialize
  renderTable();
  updateLiveBill();
</script>
</body>
</html>
```